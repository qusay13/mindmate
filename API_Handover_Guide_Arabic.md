# 📱 دليل تسليم الـ API لمطور الجوال (MindMate)

أهلاً بك يا بطل! بما أننا في مرحلة التطوير المحلي (Localhost)، فقد قمنا بتجهيز "حزمة ملفات" متكاملة لتبدأ العمل فوراً دون الحاجة للاتصال المباشر بسيرفرنا.

## 📁 الملفات المرسلة لك:
1. **`schema.json` / `schema.yml`**: ملفات التوثيق الرسمية (OpenAPI 3.0).
2. **`MindMate_Environment.postman_environment.json`**: ملف إعدادات البيئة لـ Postman.

---

## 1. كيف تشاهد التوثيق التفاعلي (Swagger)؟
بما أن السيرفر يعمل محلياً، يمكنك رؤية التوثيق الجرافيكي الجميل باتباع الآتي:
1. اذهب إلى موقع **[Swagger Editor](https://editor.swagger.io/)**.
2. من القائمة اختر **File** ثم **Import File**.
3. اختر ملف **`schema.yml`** الذي أرسلته لك.
4. سيظهر لك التوثيق كاملاً على اليمين مع شرح كل Request و Response.

---

## 2. إعداد Postman (أسرع طريقة للبدء)
لقد جهزنا لك كل الطلبات (Requests) لتعمل بضغطة زر:

### أ. استيراد الملفات:
1. افتح تطبيق **Postman**.
2. اضغط على زر **Import** في الأعلى.
3. قم بسحب وإسقاط ملف **`schema.json`**.
4. سيتعرف Postman عليه تلقائياً كـ API Collection، اضغط **Import**.

### ب. ضبط البيئة (Environment):
1. اضغط **Import** مجدداً وارفع ملف **`MindMate_Environment.postman_environment.json`**.
2. من القائمة العلوية اليمنى في Postman، اختر بيئة **MindMate Local**.
3. **ملاحظة هامة:** إذا كنت تختبر من جوال حقيقي على نفس الشبكة، قم بتغيير قيمة `base_url` في البيئة من `localhost` إلى **IP الجهاز** الذي يعمل عليه السيرفر (مثلاً: `http://192.168.1.5:8000/api`).

---

## 3. نظام المصادقة (Authentication)
نحن نستخدم **Token-Based Authentication**:
- عند تسجيل الدخول بنجاح عبر `POST /api/accounts/login/` ستحصل على `token`.
- يجب وضعه في الـ Header لكل الطلبات اللاحقة كالتالي:
  `Authorization: Bearer <your_token>`

---

## 4. ملاحظات برمجية للموبايل:
- **الصور:** الـ API يعيد مسارات مثل `/media/profiles/img.jpg`. يجب دمجها مع الـ Base URL لعرضها.
- **WebSockets:** للدردشة، استخدم البروتوكول `ws://` بدلاً من `http://`.
- **JSON:** جميع الاستجابات تأتي بتنسيق JSON موحد مع Useable Error Messages.

---
**بالتوفيق في بناء التطبيق! إذا واجهت أي مشكلة، فملفات الـ Schema هي مرجعك التقني الأول.**
